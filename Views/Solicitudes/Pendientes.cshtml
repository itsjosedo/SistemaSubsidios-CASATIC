@model IEnumerable<Subsidio>
@{
    ViewData["Title"] = "Solicitudes pendientes";
    Layout = ViewBag.Layout ?? "_Layout";
    var esEntidad = ViewBag.EsEntidad ?? false;
}

<div class="container-fluid px-4">
    <!-- Header -->
    <div class="row align-items-center mb-4">
        <div class="col-12 col-md-8 mb-3 mb-md-0">
            <h1 class="h3 mb-0 text-gray-800">
                <i class="fas fa-clock me-2 text-warning"></i>@ViewData["Title"]
            </h1>
            <p class="mb-0 text-muted small">
                @if (esEntidad)
                {
                    <text>Solicitudes de subsidios pendientes creadas por mi entidad</text>
                }
                else
                {
                    <text>Todas las solicitudes de subsidios pendientes de aprobación</text>
                }
            </p>
        </div>
        
        <div class="col-12 col-md-4">
            <div class="d-flex gap-2 justify-content-start justify-content-md-end">
                <a asp-controller="Subsidios" asp-action="Index" class="btn btn-secondary btn-sm shadow-sm">
                    <i class="fas fa-arrow-left me-1"></i>
                    <span>Volver a subsidios</span>
                </a>
            </div>
        </div>
    </div>

    <div class="card shadow">
        <div class="card-body p-0 p-sm-3">
            @if (Model.Any())
            {
                <!-- 💻 VISTA DESKTOP (Tabla completa) -->
                <div class="table-responsive d-none d-lg-block">
                    <table class="table table-bordered table-hover mb-0" width="100%" cellspacing="0">
                        <thead class="bg-gradient-warning text-white">
                            <tr>
                                <th>Programa</th>
                                <th>Tipo</th>
                                <th>Beneficiarios</th>
                                <th class="text-end">Monto</th>
                                <th class="text-center">Fecha solicitud</th>
                                <th class="text-center">Estado</th>
                                <th class="text-center">Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var solicitud in Model)
                            {
                                <tr>
                                    <td>
                                        <div class="d-flex align-items-center">
                                            <div class="avatar-sm me-3">
                                                <div class="avatar-initials bg-warning text-white rounded-circle d-flex align-items-center justify-content-center"
                                                    style="width: 40px; height: 40px; font-weight: 600;">
                                                    @GetProgramaInitials(solicitud.NombrePrograma)
                                                </div>
                                            </div>
                                            <div>
                                                <h6 class="mb-1">@solicitud.NombrePrograma</h6>
                                                <small class="text-muted">ID: #@solicitud.Id</small>
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="badge bg-info text-white">
                                            <i class="fas fa-tag me-1"></i>@solicitud.Tipo
                                        </span>
                                    </td>
                                    <td>
                                        @if (solicitud.Beneficiarios.Any())
                                        {
                                            <div>
                                                <h6 class="mb-1">@solicitud.Beneficiarios.Count beneficiario(s)</h6>
                                                <small class="text-muted">
                                                    <i class="fas fa-users me-1"></i>
                                                    @string.Join(", ", solicitud.Beneficiarios.Take(2).Select(b => b.Nombre))
                                                    @if (solicitud.Beneficiarios.Count > 2)
                                                    {
                                                        <text> y @(solicitud.Beneficiarios.Count - 2) más</text>
                                                    }
                                                </small>
                                            </div>
                                        }
                                        else
                                        {
                                            <span class="text-muted">No asignado</span>
                                        }
                                    </td>
                                    <td class="text-end">
                                        <span class="fw-bold text-warning">$@solicitud.Monto.ToString("N2")</span>
                                    </td>
                                    <td class="text-center">
                                        <i class="fas fa-calendar me-2 text-muted"></i>
                                        @solicitud.FechaAsignacion.ToString("dd/MM/yyyy")
                                    </td>
                                    <td class="text-center">
                                        <span class="badge bg-warning">
                                            <i class="fas fa-clock me-1"></i>@solicitud.Estado
                                        </span>
                                    </td>
                                    <td class="text-center">
                                        <a asp-controller="Subsidios" asp-action="Details" asp-route-id="@solicitud.Id" 
                                           class="btn btn-sm btn-outline-primary" title="Ver detalles">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <!-- 📱 VISTA MÓVIL (Lista expandible) -->
                <div class="d-lg-none">
                    @foreach (var solicitud in Model)
                    {
                        var itemId = $"solicitud-{solicitud.Id}";
                        
                        <div class="border-bottom solicitud-item">
                            <!-- Fila principal (siempre visible) -->
                            <div class="p-3 cursor-pointer" data-bs-toggle="collapse" data-bs-target="#@itemId" aria-expanded="false">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div class="flex-grow-1">
                                        <div class="d-flex align-items-center mb-2">
                                            <div class="avatar-initials bg-warning text-white rounded-circle d-flex align-items-center justify-content-center me-2"
                                                style="width: 32px; height: 32px; font-size: 0.75rem; font-weight: 600;">
                                                @GetProgramaInitials(solicitud.NombrePrograma)
                                            </div>
                                            <div>
                                                <h6 class="mb-0 fw-bold">@solicitud.NombrePrograma</h6>
                                                <small class="text-muted">ID: #@solicitud.Id</small>
                                            </div>
                                        </div>
                                        <div class="d-flex align-items-center gap-2 flex-wrap">
                                            <span class="badge bg-warning">
                                                <i class="fas fa-clock me-1"></i>@solicitud.Estado
                                            </span>
                                            <span class="badge bg-info">
                                                <i class="fas fa-tag me-1"></i>@solicitud.Tipo
                                            </span>
                                            <span class="fw-bold text-warning">$@solicitud.Monto.ToString("N2")</span>
                                        </div>
                                        <!-- Estado de beneficiarios en vista móvil -->
                                        <div class="mt-2">
                                            @if (solicitud.Beneficiarios.Any())
                                            {
                                                <span class="badge bg-primary text-white">
                                                    <i class="fas fa-users me-1"></i>@solicitud.Beneficiarios.Count beneficiario(s)
                                                </span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-secondary text-white">
                                                    <i class="fas fa-user-times me-1"></i>Sin beneficiarios
                                                </span>
                                            }
                                        </div>
                                    </div>
                                    <i class="fas fa-chevron-down text-muted ms-2"></i>
                                </div>
                            </div>

                            <!-- Detalles expandibles -->
                            <div class="collapse" id="@itemId">
                                <div class="px-3 pb-3 bg-light">
                                    <div class="mb-2">
                                        <small class="text-muted d-block">Beneficiarios (@solicitud.Beneficiarios.Count)</small>
                                        @if (solicitud.Beneficiarios.Any())
                                        {
                                            <div class="mt-1">
                                                @foreach (var beneficiario in solicitud.Beneficiarios.Take(3))
                                                {
                                                    <div class="d-flex justify-content-between align-items-center py-1 border-bottom">
                                                        <div>
                                                            <strong>@beneficiario.Nombre</strong>
                                                            <small class="text-muted d-block">
                                                                <i class="fas fa-id-card me-1"></i>@beneficiario.Dui
                                                            </small>
                                                        </div>
                                                    </div>
                                                }
                                                @if (solicitud.Beneficiarios.Count > 3)
                                                {
                                                    <small class="text-muted">+ @(solicitud.Beneficiarios.Count - 3) más...</small>
                                                }
                                            </div>
                                        }
                                        else
                                        {
                                            <span class="text-muted">No asignado</span>
                                        }
                                    </div>
                                    <div class="mb-3">
                                        <small class="text-muted d-block">Fecha de solicitud</small>
                                        <strong>
                                            <i class="fas fa-calendar me-1"></i>
                                            @solicitud.FechaAsignacion.ToString("dd/MM/yyyy")
                                        </strong>
                                    </div>
                                    <div class="mb-3">
                                        <small class="text-muted d-block">Tipo de subsidio</small>
                                        <span class="badge bg-info">
                                            <i class="fas fa-tag me-1"></i>@solicitud.Tipo
                                        </span>
                                    </div>
                                    <div class="mb-3">
                                        <small class="text-muted d-block">Estado</small>
                                        <span class="badge bg-warning">
                                            <i class="fas fa-clock me-1"></i>@solicitud.Estado
                                        </span>
                                    </div>
                                    <div class="mb-0">
                                        <small class="text-muted d-block">Monto solicitado</small>
                                        <h5 class="text-warning mb-0">$@solicitud.Monto.ToString("N2")</h5>
                                    </div>

                                    <!-- Botón de acción -->
                                    <div class="d-grid mt-3">
                                        <a asp-controller="Subsidios" asp-action="Details" asp-route-id="@solicitud.Id" 
                                           class="btn btn-primary btn-sm">
                                            <i class="fas fa-eye me-2"></i>Ver detalles completos
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <div class="empty-state">
                        <i class="fas fa-check-circle fa-4x text-success mb-3"></i>
                        <h4 class="text-muted">No hay solicitudes pendientes</h4>
                        <p class="text-muted mb-4">
                            @if (esEntidad)
                            {
                                <text>Su entidad no tiene solicitudes pendientes en este momento.</text>
                            }
                            else
                            {
                                <text>Todas las solicitudes han sido procesadas.</text>
                            }
                        </p>
                        <a asp-controller="Subsidios" asp-action="Index" class="btn btn-secondary">
                            <i class="fas fa-arrow-left me-2"></i>Volver a subsidios
                        </a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Animar el ícono de chevron cuando se expande/colapsa
        document.addEventListener('DOMContentLoaded', function() {
            const collapseElements = document.querySelectorAll('.collapse');
            collapseElements.forEach(function(element) {
                element.addEventListener('show.bs.collapse', function() {
                    const chevron = this.previousElementSibling.querySelector('.fa-chevron-down');
                    if (chevron) chevron.classList.add('rotate-180');
                });
                element.addEventListener('hide.bs.collapse', function() {
                    const chevron = this.previousElementSibling.querySelector('.fa-chevron-down');
                    if (chevron) chevron.classList.remove('rotate-180');
                });
            });
        });
    </script>
    
    <style>
        .cursor-pointer {
            cursor: pointer;
        }
        .fa-chevron-down {
            transition: transform 0.3s ease;
        }
        .rotate-180 {
            transform: rotate(180deg);
        }
        .solicitud-item:hover {
            background-color: #f8f9fa;
        }
    </style>
}

@functions {
    public string GetProgramaInitials(string nombrePrograma)
    {
        if (string.IsNullOrEmpty(nombrePrograma)) return "SP";
        var palabras = nombrePrograma.Split(' ', StringSplitOptions.RemoveEmptyEntries);
        if (palabras.Length >= 2)
        {
            return $"{palabras[0][0]}{palabras[1][0]}".ToUpper();
        }
        return nombrePrograma.Length >= 2 ? nombrePrograma.Substring(0, 2).ToUpper() : nombrePrograma.ToUpper();
    }
}
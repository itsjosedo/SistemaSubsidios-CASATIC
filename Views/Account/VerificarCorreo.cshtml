@{
    ViewData["Title"] = "Verificación correo";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="card shadow-lg p-4" style="max-width: 400px; width: 100%; border-radius: 15px;">

        <div class="text-center mb-3">
            <i class="fas fa-key text-primary" style="font-size: 45px;"></i>
            <h3 class="mt-2">Restablecer contraseña</h3>
            <p class="text-muted">
                Ingresa el correo asociado a tu cuenta.
            </p>
        </div>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger text-center p-2">
                <i class="fas fa-times-circle me-1"></i> @ViewBag.Error
            </div>
        }

        <form method="post">
            <div class="mb-3">
                <label class="form-label">Correo electrónico</label>
                <input type="email" name="correo" class="form-control text-center" 
                       placeholder="ejemplo@gmail.com"
                       required />
            </div>

            <button class="btn btn-primary w-100 py-2">
                <i class="fas fa-paper-plane me-1"></i> Enviar código
            </button>
        </form>

    </div>
</div>
